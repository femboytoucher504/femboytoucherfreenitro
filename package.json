{
  "name": "freenitroemojis",
  "type": "module",
  "scripts": {
    "build": "esbuild index.ts --bundle --minify --format=iife --global-name=plugin --external:@vendetta --external:@vendetta/* --outfile=index.js && echo 'plugin;' >> index.js && mkdir -p dist && cp index.js dist/index.js && cp manifest.json dist/manifest.json"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
